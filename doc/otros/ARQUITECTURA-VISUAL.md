# 🏗️ Arquitectura Visual Completa

## Sistema Completo - Vista General

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         APLICACIÓN .NET (puerto 5000)                        │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  Controllers                                                            │  │
│  │  - GET  /example/info                                                 │  │
│  │  - POST /example/process  (genera logs + métricas)                   │  │
│  │  - POST /example/transaction (más logs + métricas)                  │  │
│  │  - GET  /metrics (endpoint Prometheus)                              │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  ExampleService (Logging de negocio)                                  │  │
│  │  ├─ ProcessOperationAsync()                                           │  │
│  │  │  ├─ LogContext.PushProperty("OperationId", operationId)           │  │
│  │  │  ├─ LogContext.PushProperty("AccountNumber", accountNumber)       │  │
│  │  │  ├─ LogContext.PushProperty("Amount", amount)                     │  │
│  │  │  ├─ log.Information("OPERATION_START")                            │  │
│  │  │  └─ log.Information("OPERATION_SUCCESS | duration_ms={Duration}") │  │
│  │  └─ ProcessTransactionAsync() (similar)                              │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │  Program.cs - Serilog Configuration                                   │  │
│  │  ├─ MinimumLevel: Information                                         │  │
│  │  ├─ Enrich: FromLogContext, WithEnvironmentName                      │  │
│  │  ├─ Sink 1: Console (JSON)                                           │  │
│  │  └─ Sink 2: GrafanaLoki (http://loki:3100)                          │  │
│  │      ├─ Labels: job=prueba-prometheus                                │  │
│  │      ├─ Labels: env=development                                      │  │
│  │      └─ Labels: service=api                                          │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────────┘
         │                                    │
         │ (JSON logs con contexto)           │ (Métricas expuestas)
         │                                    │
         ▼                                    ▼
┌──────────────────────┐            ┌─────────────────────┐
│  GRAFANA LOKI 3.0.0  │            │  PROMETHEUS 2.45.0  │
│  (puerto 3100)       │            │  (puerto 9090)      │
│                      │            │                     │
│  ┌────────────────┐  │            │  ┌───────────────┐  │
│  │ Distributor    │  │            │  │ Scraper       │  │
│  │ (recibe logs)  │  │            │  │ (puerto 5000) │  │
│  └────────────────┘  │            │  └───────────────┘  │
│  ┌────────────────┐  │            │  ┌───────────────┐  │
│  │ Ingester       │  │            │  │ TSDB          │  │
│  │ (procesa logs) │  │            │  │ (almacena     │  │
│  └────────────────┘  │            │  │  series)      │  │
│  ┌────────────────┐  │            │  └───────────────┘  │
│  │ Querier        │  │            │  ┌───────────────┐  │
│  │ (indexación)   │  │            │  │ Alertmanager  │  │
│  └────────────────┘  │            │  │ (alertas)     │  │
│                      │            │                     │
└──────────────────────┘            └─────────────────────┘
         │
         │ (índices y chunks)
         │
         ▼
┌──────────────────────────┐
│  MinIO S3-COMPATIBLE     │
│  (puerto 9000/9001)      │
│                          │
│  Bucket: loki            │
│  ├─ índices              │
│  ├─ chunks               │
│  └─ WAL (write-ahead)    │
│                          │
│  Storage backend         │
│  para Loki               │
└──────────────────────────┘

         ▲                                    ▲
         │ (Lee datos)                        │ (Lee datos)
         │                                    │
┌────────────────────────────────────────────────────────────────┐
│                    GRAFANA 10.0.0                              │
│                    (puerto 3000)                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ DataSources (auto-provisioned)                          │ │
│  │ ├─ Prometheus: http://prometheus:9090 (default)        │ │
│  │ └─ Loki: http://loki:3100 (logs)                       │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ Explore (Query Builder)                                 │ │
│  │ ├─ LogQL queries: {job="prueba-prometheus"}            │ │
│  │ └─ PromQL queries: rate(operations_total[1m])          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ Dashboards (auto-provisioned)                           │ │
│  │ └─ dotnet-metrics: Operaciones, transacciones, etc.    │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ Alerting                                                │ │
│  │ └─ Crear alerts basadas en LogQL/PromQL               │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Login: admin / admin123                                       │
└────────────────────────────────────────────────────────────────┘

         ▲
         │ (Usuario final)
         │
    ┌────────────┐
    │  Navegador │
    │ localhost  │
    │:3000       │
    └────────────┘
```

---

## Flujo de Datos - Paso a Paso

### 1. Generación de Logs
```
Usuario hace request
  ↓
Controller/ExampleService
  ↓
logger.Information("OPERATION_SUCCESS | duration_ms=150")
  ↓
Serilog (en memoria)
  ├─ Enriquece con LogContext (OperationId, AccountNumber, etc.)
  ├─ Serializa a JSON
  └─ Batching (1000 eventos o 5 segundos)
```

### 2. Envío a Loki
```
Batch de logs JSON
  ↓
Serilog GrafanaLoki Sink
  ├─ HTTP POST a http://loki:3100/loki/api/v1/push
  ├─ Headers: Content-Type: application/json
  └─ Body: 
     {
       "streams": [{
         "stream": {"job": "prueba-prometheus", "env": "development"},
         "values": [[timestamp, json_log], ...]
       }]
     }
```

### 3. Procesamiento en Loki
```
Loki Distributor recibe POST
  ↓
Validación y parsing
  ↓
Loki Ingester
  ├─ Empieza compresión de chunks
  ├─ Mantiene en memoria WAL
  └─ Envía a almacenamiento (MinIO)
```

### 4. Almacenamiento en MinIO
```
Loki persiste
  ↓
MinIO S3 API
  ├─ Bucket: loki
  ├─ Paths: 
  │  ├─ loki/index/...
  │  ├─ loki/chunks/...
  │  └─ loki/wal/...
  └─ Volumen: minio_data (persistente)
```

### 5. Consulta desde Grafana
```
Usuario abre Grafana
  ↓
Explore tab
  ↓
Data Source: Loki
  ↓
Query: {job="prueba-prometheus"}
  ↓
Grafana hace HTTP GET a Loki
  ↓
Loki Querier busca en índices (TSDB)
  ↓
Retorna logs encontrados
  ↓
Grafana renderiza en UI
```

---

## Componentes Internos Detallados

### Serilog Pipeline
```
Log Event
  ↓
┌─────────────────────────┐
│ Enrichers               │
├─────────────────────────┤
│ FromLogContext()        │ → OperationId, AccountNumber, etc.
│ WithEnvironmentName()   │ → development
└─────────────────────────┘
  ↓
┌─────────────────────────┐
│ Sinks (simultáneos)     │
├─────────────────────────┤
│ 1. Console              │ → Salida local
│ 2. GrafanaLoki          │ → HTTP a Loki
└─────────────────────────┘
```

### Loki Components
```
┌──────────────────────────────────────────┐
│         HTTP API (puerto 3100)           │
│  - /loki/api/v1/push  (push logs)       │
│  - /loki/api/v1/query_range  (query)   │
│  - /ready (health check)                │
└──────────────────────────────────────────┘
  ↓
┌──────────────────────────────────────────┐
│ Distributor (HAProxy-like)               │
│ - Valida request                         │
│ - Distribuye a ingestores               │
└──────────────────────────────────────────┘
  ↓
┌──────────────────────────────────────────┐
│ Ingester (stateful)                      │
│ - Comprime chunks                        │
│ - Mantiene WAL                           │
│ - Buffers en memoria                     │
└──────────────────────────────────────────┘
  ↓
┌──────────────────────────────────────────┐
│ Storage (MinIO S3)                       │
│ - Chunks (logs comprimidos)              │
│ - Índices TSDB                           │
│ - WAL para recuperación                  │
└──────────────────────────────────────────┘
  ↑
┌──────────────────────────────────────────┐
│ Querier                                  │
│ - Lee desde ingester (reciente)          │
│ - Lee desde storage (histórico)          │
│ - Merges resultados                      │
└──────────────────────────────────────────┘
```

### Grafana Architecture
```
┌─────────────────────────────────────────┐
│  Frontend (React/TypeScript)            │
│  - Dashboard UI                         │
│  - Explore builder                      │
│  - Alert manager                        │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│  Backend API (Go)                       │
│  - Autenticación                        │
│  - Datasource management                │
│  - Query execution                      │
└─────────────────────────────────────────┘
  ↓
┌──────────────────┐  ┌──────────────────┐
│  Prometheus SDK  │  │  Loki SDK        │
│  (PromQL)        │  │  (LogQL)         │
└──────────────────┘  └──────────────────┘
  ↓                     ↓
┌──────────────────┐  ┌──────────────────┐
│ Prometheus       │  │ Loki             │
│ (metrics)        │  │ (logs)           │
└──────────────────┘  └──────────────────┘
```

---

## Volúmenes Docker

```
┌──────────────────────────────────────────────────┐
│ Docker Volumes (persistencia)                   │
├──────────────────────────────────────────────────┤
│ prometheus_data                                  │
│ └─ metrics TSDB files                           │
│                                                  │
│ grafana_data                                     │
│ └─ dashboards, datasources, users               │
│                                                  │
│ loki_data                                        │
│ └─ (vacío, Loki usa MinIO)                      │
│                                                  │
│ minio_data                                       │
│ └─ loki/                                        │
│    ├─ index/                                    │
│    ├─ chunks/                                   │
│    └─ wal/                                      │
└──────────────────────────────────────────────────┘
```

---

## Red Docker (Bridge)

```
Docker Network: pruebaprometheus_monitoring

┌─────────────────────────────────────────────────┐
│ prometheus:9090      (scrapes metrics)          │
│ grafana:3000         (UI dashboard)             │
│ loki:3100            (logs aggregation)         │
│ minio:9000           (S3 storage)               │
│ minio:9001           (web console)              │
└─────────────────────────────────────────────────┘
        ↑
        │ Localhost port mapping
        │
┌──────────────────────────────────────────────────┐
│ Host Machine (Windows)                          │
│ ├─ localhost:3000   → grafana                   │
│ ├─ localhost:5000   → app .NET                  │
│ ├─ localhost:9001   → minio console             │
│ └─ localhost:9090   → prometheus                │
└──────────────────────────────────────────────────┘
```

---

## Métricas Generadas

```
Aplicación .NET
  ├─ http_request_duration_ms (Histogram)
  ├─ http_requests_total (Counter)
  ├─ operations_total (Counter)
  ├─ operation_processing_duration_ms (Histogram)
  ├─ transactions_total (Counter)
  ├─ transactions_by_type (Counter)
  ├─ transactions_by_path (Counter)
  └─ transaction_processing_duration_ms (Histogram)

Prometheus internamente
  ├─ up (1 = scrape successful)
  ├─ scrape_duration_seconds
  ├─ scrape_samples_scraped
  └─ ...

Loki internamente
  ├─ loki_ingester_chunks (Counter)
  ├─ loki_ingester_bytes_received (Counter)
  └─ ...
```

---

## Seguridad (Recomendaciones para Producción)

```
┌────────────────────────────────────────┐
│ DMZ / Reverse Proxy                    │
│ (nginx, traefik, etc.)                 │
└────────────────────────────────────────┘
         ↓ (SSL/TLS)
┌────────────────────────────────────────┐
│ Aplicación .NET                        │
│ ├─ Auth: JWT/OAuth2                    │
│ └─ HTTPS only                          │
└────────────────────────────────────────┘
         ↓ (mTLS)
┌────────────────────────────────────────┐
│ Stack de Observabilidad                │
│ ├─ Prometheus: lectura HTTP             │
│ ├─ Loki: HTTP con rate limiting        │
│ ├─ Grafana: HTTPS + Auth               │
│ └─ MinIO: HTTPS + TLS                  │
└────────────────────────────────────────┘
```

---

*Diagrama generado: 16 de enero de 2026*
